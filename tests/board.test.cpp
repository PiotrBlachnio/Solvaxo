#include "test.h"

TEST_CASE("Board") {
    SECTION("Constructor") {
        SECTION("Should transform input string to the board's data and assign in to the struct's field") {
            
        }

        SECTION("Should throw an exception when input string is invalid") {

        }
    }
}