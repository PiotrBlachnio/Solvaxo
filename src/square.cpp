#include "square.h"

//TODO: Throw an error if parameters are not valid

Square::Square(int number, int rowIndex, int columnIndex) {
    this->number = number;
    this->rowIndex = rowIndex;
    this->columnIndex = columnIndex;
}